the multiproof ships end to end: shell builds a pruned tree (mirrors pair_forest, collapses fully-hidden subtrees bottom-up), the extracted mp_verify checks it, the wire format is the tagged tree, format.md and the sixty-line Python verifier reimplement it, v1 retired. Measured: 32-leaf finding with two revealed spans ships 12-14 nodes not 32; a forged reveal and a tampered hidden hash both rejected by the independent verifier. Building the tree is untrusted shell, so a bug there can only fail an honest disclosure, never pass a forged one.
